# Microsoft Sentinel → canonical taxonomy mappings.
#
# Five tiers, resolved in order (see vendors/sentinel.py for full docs):
#   1. kql_tables       — first KQL table the rule queries (authoritative)
#   2. connectors       — `requiredDataConnectors[].connectorId`
#   3. data_types       — `requiredDataConnectors[].dataTypes[]`
#   4. solution_folders — `Solutions/<vendor>/` vendor fallback
#   5. entity_types     — `entityMappings[].entityType` last-resort hint
#
# Keys are lowercase. Tier 1 is the dominant signal; every Sentinel
# rule starts with a table. Tier 4 covers the long tail of third-party
# vendors from the Marketplace that don't declare requiredDataConnectors.

# ─────────────────────────────────────────────────────────────────────────
# TIER 1 — KQL TABLE NAMES (authoritative event_type)
# The first table referenced at the head of the query. This is the
# most reliable data-source signal we have. `*_cl` catches any vendor
# custom log; specific `_CL` tables (e.g. BHEAttackPathsData_CL) can be
# listed explicitly if more precision is wanted.
# ─────────────────────────────────────────────────────────────────────────
kql_tables:
  # ── Microsoft Entra / Azure AD ──────────────────────────────────────
  auditlogs:
    platforms: [azure]
    data_sources: [entra_id_audit]
    event_types: [audit_event]
  signinlogs:
    platforms: [azure]
    data_sources: [entra_id_signin]
    event_types: [authentication]
  aadnoninteractiveusersigninlogs:
    platforms: [azure]
    data_sources: [entra_id_signin]
    event_types: [authentication]
  aadprovisioninglogs:
    platforms: [azure]
    data_sources: [entra_id_audit]
    event_types: [audit_event]
  aadservicepricinciplesigninlogs:  # typo that appears in some rules
    platforms: [azure]
    data_sources: [entra_id_signin]
    event_types: [authentication]
  aadserviceprincipalsigninlogs:
    platforms: [azure]
    data_sources: [entra_id_signin]
    event_types: [authentication]
  aadmanagedidentitysigninlogs:
    platforms: [azure]
    data_sources: [entra_id_signin]
    event_types: [authentication]
  identityinfo:
    platforms: [azure]
    data_sources: [entra_id_audit]
    event_types: [audit_event]
  # ── Azure platform ───────────────────────────────────────────────────
  azureactivity:
    platforms: [azure]
    data_sources: [azure_activity]
    event_types: [api_call]
  azurediagnostics:
    platforms: [azure]
    data_sources: [azure_activity]
    event_types: [api_call]
  # ── Microsoft 365 / Office ───────────────────────────────────────────
  officeactivity:
    platforms: [microsoft_365]
    data_sources: [m365_audit]
    event_types: [audit_event]
  cloudappevents:
    platforms: [microsoft_365]
    data_sources: [m365_audit]
    event_types: [audit_event]
  emailevents:
    platforms: [microsoft_365]
    data_sources: [m365_exchange_audit]
    event_types: [email_message]
  emailattachmentinfo:
    platforms: [microsoft_365]
    data_sources: [m365_exchange_audit]
    event_types: [email_message]
  emailurlinfo:
    platforms: [microsoft_365]
    data_sources: [m365_exchange_audit]
    event_types: [email_message]
  dataverseactivity:
    platforms: [microsoft_365]
    data_sources: [m365_audit]
    event_types: [audit_event]
  # ── Microsoft Defender for Endpoint (Device* tables) ────────────────
  deviceevents:
    platforms: [windows]
    data_sources: [defender_endpoint]
    event_types: [audit_event]
  deviceprocessevents:
    platforms: [windows]
    data_sources: [defender_endpoint]
    event_types: [process_creation]
  devicenetworkevents:
    platforms: [windows]
    data_sources: [defender_endpoint]
    event_types: [network_connection]
  devicefileevents:
    platforms: [windows]
    data_sources: [defender_endpoint]
    event_types: [file_event]
  deviceregistryevents:
    platforms: [windows]
    data_sources: [defender_endpoint]
    event_types: [registry_event]
  deviceimageloadevents:
    platforms: [windows]
    data_sources: [defender_endpoint]
    event_types: [image_load]
  devicelogonevents:
    platforms: [windows]
    data_sources: [defender_endpoint]
    event_types: [authentication]
  deviceinfo:
    platforms: [windows]
    data_sources: [defender_endpoint]
    event_types: [audit_event]
  # ── Windows native / Syslog ──────────────────────────────────────────
  securityevent:
    platforms: [windows]
    data_sources: [windows_security_event_log]
    event_types: [audit_event]
  windowsevent:
    platforms: [windows]
    data_sources: [windows_security_event_log]
    event_types: [audit_event]
  windowsfirewall:
    platforms: [windows]
    data_sources: [windows_security_event_log]
    event_types: [network_connection]
  syslog:
    platforms: [linux]
    data_sources: [linux_syslog]
    event_types: [audit_event]
  # ── CEF / Azure Monitor Agent ingest ─────────────────────────────────
  commonsecuritylog:
    # CEF pipe. Wide variety of appliances; platform left broad.
    platforms: [network_appliance]
    data_sources: [cisco_firewall, palo_alto_firewall, fortinet_firewall]
    event_types: [network_connection]
  # ── AWS ──────────────────────────────────────────────────────────────
  awscloudtrail:
    platforms: [aws]
    data_sources: [aws_cloudtrail]
    event_types: [api_call]
  awsguardduty:
    platforms: [aws]
    data_sources: [aws_guardduty]
    event_types: [audit_event]
  awscloudwatch:
    platforms: [aws]
    data_sources: [aws_cloudtrail]
    event_types: [api_call]
  awsvpcflow:
    platforms: [aws]
    data_sources: [aws_vpc_flow]
    event_types: [network_connection]
  # ── GCP ──────────────────────────────────────────────────────────────
  gcp_audit_logs:
    platforms: [gcp]
    data_sources: [gcp_audit]
    event_types: [api_call]
  # ── SIEM alert streams (Sentinel's own + external) ───────────────────
  # SecurityAlert is Sentinel's central alert table. Sources include
  # Defender for Endpoint (windows), Defender for Cloud (azure),
  # Defender for Cloud Apps + M365 Defender + Defender for Identity
  # (microsoft_365). AWS Security Hub findings can land here too via
  # connector but the overwhelmingly common case is Microsoft.
  securityalert:
    platforms: [microsoft_365, azure, windows]
    data_sources: [siem_alert]
    event_types: [audit_event]
  securityincident:
    platforms: [microsoft_365, azure, windows]
    data_sources: [siem_alert]
    event_types: [alert_correlation]
  # Defender for Cloud security recommendations (Azure-resident).
  securitynestedrecommendation:
    platforms: [azure]
    data_sources: [siem_alert]
    event_types: [audit_event]
  # Sentinel UEBA -- analyzes Entra / SecurityEvent / signinlogs data
  # but the table itself is an Azure-resident feature.
  behavioranalytics:
    platforms: [azure]
    data_sources: [siem_alert]
    event_types: [audit_event]
  # Sentinel ML anomalies -- Azure-resident detection service.
  anomalies:
    platforms: [azure]
    data_sources: [siem_alert]
    event_types: [audit_event]
  # ── Threat intelligence ──────────────────────────────────────────────
  # MS Sentinel TI / MDTI ingest -- Azure-resident table. Describes
  # IOCs that apply to many platforms; the table itself lives in Azure.
  threatintelligenceindicator:
    platforms: [azure]
    data_sources: [siem_alert]
    event_types: [audit_event]
  threatintelindicators:
    platforms: [azure]
    data_sources: [siem_alert]
    event_types: [audit_event]
  # ── Azure DevOps ─────────────────────────────────────────────────────
  # Azure DevOps audit logs -- Azure service.
  adoauditlogs:
    platforms: [azure]
    data_sources: [application_logs]
    event_types: [audit_event]
  # ── GitHub ───────────────────────────────────────────────────────────
  githubauditlogpolling_cl:
    platforms: [github]
    data_sources: [github_audit]
    event_types: [audit_event]
  # ── Okta / SaaS identity ─────────────────────────────────────────────
  okta_cl:
    platforms: [okta]
    data_sources: [okta_system_log]
    event_types: [authentication]
  oktav2_cl:
    platforms: [okta]
    data_sources: [okta_system_log]
    event_types: [authentication]
  # ── Dedicated third-party tables (avoid `_cl` fallback genericness) ──
  bheattackpathsdata_cl:
    platforms: [windows]
    data_sources: [siem_alert]
    event_types: [alert_correlation]
  cyfirmaindicators_cl:
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]
  cyblevisionalerts_cl:
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]
  aishield:  # table (no _CL suffix in this case)
    platforms: [llm]
    data_sources: [siem_alert]
    event_types: [audit_event]
  aishield_cl:
    platforms: [llm]
    data_sources: [siem_alert]
    event_types: [audit_event]
  theomalerts_cl:
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]
  authomize_v2_cl:
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]
  senservapro_cl:
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]
  cognniincidents_cl:
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]
  cloudflare:
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [network_connection]
  veeamonetriggeredalarms_cl:
    platforms: [cross_platform]
    data_sources: [application_logs]
    event_types: [audit_event]
  # Veeam backup's own ingested syslog stream — Veeam server runs
  # on Windows; the syslog IS application_logs, NOT linux_syslog.
  # (Previously also tagged cross_platform; redundant -- dropped.)
  veeam_getsecurityevents:
    platforms: [windows]
    data_sources: [application_logs]
    event_types: [audit_event]
  # ── Miscellaneous vendor tables we saw in the review ─────────────────
  netbackupalerts_cl:
    platforms: [cross_platform]
    data_sources: [application_logs]
    event_types: [audit_event]
  sapbtpauditlog_cl:
    platforms: [cross_platform]
    data_sources: [application_logs]
    event_types: [audit_event]
  # ── Custom log catch-all (any `*_CL` table not listed above) ────────
  # Platform deliberately empty -- let connectors / solution_folders
  # tiers determine platform. Tagging every unknown _CL table as
  # `cross_platform` was poisoning ~50% of Sentinel's distribution.
  # ── Drift 2026-08-30 (#64 + prod unknown-data-source audit) ─────────
  gcpclouddns:
    platforms: [gcp]
    data_sources: [gcp_dns]
    event_types: [dns_query]
  gcp_iam:
    platforms: [gcp]
    data_sources: [gcp_audit]
    event_types: [audit_event]
  paloaltoprismacloud:
    platforms: []
    data_sources: [prisma_cloud_audit]
    event_types: [platform_alert]
  cyberarkepm:
    platforms: [windows]
    data_sources: [cyberark_epm]
    event_types: [audit_event]
  jiraaudit:
    platforms: [atlassian]
    data_sources: [atlassian_audit]
    event_types: [audit_event]
  gworkspaceactivityreports:
    platforms: [google_workspace]
    data_sources: [google_workspace_audit]
    event_types: [audit_event]
  trendmicrocas:
    platforms: [email]
    data_sources: [trend_micro_cas]
    event_types: [platform_alert]
  bitglass:
    platforms: []
    data_sources: [proxy_logs]
    event_types: [http_request]
  ocilogs:
    platforms: [oci]
    data_sources: [oci_audit]
    event_types: [audit_event]
  snowflake:
    platforms: [snowflake]
    data_sources: [snowflake_query_history, snowflake_login_history]
    event_types: [audit_event]
  impervawafcloud:
    platforms: [network_appliance]
    data_sources: [imperva_waf]
    event_types: [http_request]
  radiflowevent:
    platforms: [network_appliance]
    data_sources: [third_party_security_alerts]
    event_types: [platform_alert]
  w3ciislog:
    platforms: [windows]
    data_sources: [webserver_logs]
    event_types: [http_request]
  azfwidpssignature:
    platforms: [azure]
    data_sources: [azure_firewall]
    event_types: [network_connection]
  sailpointidn_events:
    platforms: []
    data_sources: [sailpoint_idn_audit]
    event_types: [audit_event]
  copilotactivity:
    platforms: [microsoft_365]
    data_sources: [m365_audit]
    event_types: [audit_event]
  lookoutevents:
    platforms: []
    data_sources: [third_party_security_alerts]
    event_types: [platform_alert]
  zoomlogs:
    platforms: [zoom]
    data_sources: [zoom_operation, zoom_activity]
    event_types: [audit_event]
  # Log Analytics agent generic Windows event channel table.
  event:
    platforms: [windows]
    data_sources: [windows_event_logs]
    event_types: [audit_event]
  salesforceservicecloud:
    platforms: [salesforce]
    data_sources: [salesforce_realtime]
    event_types: [audit_event]
  securityrecommendation:
    platforms: [azure]
    data_sources: [defender_cloud]
    event_types: [platform_alert]
  securityregulatorycompliance:
    platforms: [azure]
    data_sources: [defender_cloud]
    event_types: [platform_alert]
  defendauditdata:  # KnowBe4 Defend
    platforms: [email]
    data_sources: [third_party_security_alerts]
    event_types: [platform_alert]
  exchangeadminauditlogs:
    platforms: [microsoft_365]
    data_sources: [m365_exchange_audit]
    event_types: [audit_event]
  purviewdatasensitivitylogs:
    platforms: [microsoft_365]
    data_sources: [m365_purview_audit]
    event_types: [audit_event]
  heartbeat:
    platforms: [azure]
    data_sources: [azure_monitor_activity]
    event_types: [audit_event]
  laquerylogs:
    platforms: [azure]
    data_sources: [azure_monitor_activity]
    event_types: [audit_event]
  appserviceantivirusscanauditlogs:
    platforms: [azure]
    data_sources: [azure_app_service]
    event_types: [platform_alert]
  asimalerteventlogs:
    platforms: []
    data_sources: [siem_alert]
    event_types: [platform_alert]
  asimalerteventbitdefendergravityzone:
    platforms: []
    data_sources: [third_party_security_alerts]
    event_types: [platform_alert]
  extrahopdetections:
    platforms: [network_appliance]
    data_sources: [third_party_security_alerts]
    event_types: [platform_alert]
  dataminrpulsealerts:
    platforms: []
    data_sources: [third_party_security_alerts]
    event_types: [platform_alert]

  # If no tier resolves a platform, the rule lands on `[unknown]`
  # which is more honest and easier to triage.
  "*_cl":
    platforms: []
    data_sources: [siem_alert]
    event_types: [audit_event]

connectors:
  # ── Long-tail vendor connectors (#4 mop-up, 2026-08-29) ──────────────
  # Each was an unmapped `sentinel:<connector>|dt:<type>` fingerprint on
  # the nightly drift report. Platforms stay cross_platform unless the
  # product is tied to one; event_types are the alert/audit shape the
  # rules actually query.
  ciscosecureendpoint:            # Cisco Secure Endpoint (AMP) EDR events
    platforms: [cross_platform]
    data_sources: [endpoint_behavior]
    event_types: [platform_alert]
  semperisdsp:                    # Semperis Directory Services Protector (AD)
    platforms: [windows]
    data_sources: [application_logs]
    event_types: [audit_event]
  googlesccdefinition:            # GCP Security Command Center findings
    platforms: [gcp]
    data_sources: [gcp_audit]
    event_types: [platform_alert]
  sentinelone:                    # SentinelOne agent / threat events
    platforms: [cross_platform]
    data_sources: [sentinelone]
    event_types: [platform_alert]
  infobloxsocinsightsdataconnector_api:
    platforms: [cross_platform]
    data_sources: [dns_query_logs]
    event_types: [platform_alert]
  infobloxsocinsightsdataconnector_ama:
    platforms: [cross_platform]
    data_sources: [dns_query_logs]
    event_types: [platform_alert]
  bitsight:                       # BitSight external ratings / findings
    platforms: [cross_platform]
    data_sources: [application_logs]
    event_types: [platform_alert]
  vectraruxconnector:             # Vectra XDR detections
    platforms: [cross_platform]
    data_sources: [network_traffic_logs]
    event_types: [platform_alert]
  lumenthreatfeedconnector:       # Lumen Defender threat feed indicators
    platforms: [cross_platform]
    data_sources: [dns_query_logs]
    event_types: [platform_alert]
  dns:                            # Windows DNS Server analytic logs
    platforms: [windows]
    data_sources: [dns_query_logs]
    event_types: [dns_query]
  versaseccms:                    # Versasec credential management system
    platforms: [cross_platform]
    data_sources: [application_logs]
    event_types: [authentication]
  ciscosdwan:                     # Cisco SD-WAN syslog / UTD
    platforms: [cross_platform]
    data_sources: [network_traffic_logs]
    event_types: [platform_alert]
  qualysvmlogsccpdefinition:      # Qualys VM host detections
    platforms: [cross_platform]
    data_sources: [application_logs]
    event_types: [platform_alert]
  # ── AWS ───────────────────────────────────────────────────────────────
  awss3:
    platforms: [aws]
    data_sources: [aws_cloudtrail]
    event_types: [api_call]
  aws:
    platforms: [aws]
    data_sources: [aws_cloudtrail]
    event_types: [api_call]
  awssecurityhub:
    platforms: [aws]
    data_sources: [aws_security_hub]
    event_types: [audit_event]
  amazonwebservicescloudtrail:
    platforms: [aws]
    data_sources: [aws_cloudtrail]
    event_types: [api_call]
  # ── Azure ─────────────────────────────────────────────────────────────
  azureactivity:
    platforms: [azure]
    data_sources: [azure_activity]
    event_types: [api_call]
  azureactivedirectory:
    platforms: [azure]
    data_sources: [entra_id_signin, entra_id_audit]
    event_types: [authentication, audit_event]
  azuread:
    platforms: [azure]
    data_sources: [entra_id_signin, entra_id_audit]
    event_types: [authentication, audit_event]
  entraid:
    platforms: [azure]
    data_sources: [entra_id_signin]
    event_types: [authentication]
  azureadidentityprotection:
    platforms: [azure]
    data_sources: [entra_id_signin]
    event_types: [authentication]
  # ── Microsoft 365 / Defender ─────────────────────────────────────────
  office365:
    platforms: [microsoft_365]
    data_sources: [m365_audit]
    event_types: [audit_event]
  officeatp:
    platforms: [microsoft_365]
    data_sources: [m365_exchange_audit]
    event_types: [email_message]
  microsoftthreatprotection:
    platforms: [windows, microsoft_365]
    data_sources: [defender_endpoint]
    event_types: [audit_event]
  microsoftdefenderadvancedthreatprotection:
    platforms: [windows]
    data_sources: [defender_endpoint]
    event_types: [audit_event]
  microsoftcloudappsecurity:
    platforms: [microsoft_365]
    data_sources: [m365_audit]
    event_types: [audit_event]
  azuresecuritycenter:
    platforms: [azure]
    data_sources: [defender_cloud]
    event_types: [audit_event]
  microsoftdefenderforcloudtenantbased:
    platforms: [azure]
    data_sources: [defender_cloud]
    event_types: [audit_event]
  microsoftdefenderforcloud:
    platforms: [azure]
    data_sources: [defender_cloud]
    event_types: [audit_event]
  # ── Identity / SaaS ──────────────────────────────────────────────────
  oktassocsl:
    platforms: [okta]
    data_sources: [okta_system_log]
    event_types: [authentication]
  duosecurity:
    platforms: [duo]
    data_sources: [duo_activity]
    event_types: [authentication]
  # ── Windows ──────────────────────────────────────────────────────────
  securityevents:
    platforms: [windows]
    data_sources: [windows_security_event_log]
    event_types: [authentication, audit_event]
  windowsfirewall:
    platforms: [windows]
    data_sources: [windows_security_event_log]
    event_types: [network_connection]
  # ── Network / Syslog / Generic ───────────────────────────────────────
  syslog:
    platforms: [linux]
    data_sources: [linux_syslog]
    event_types: [audit_event]
  cef:  # Common Event Format ingest — generic, often network appliances
    platforms: [network_appliance]
    data_sources: [palo_alto_firewall, fortinet_firewall, cisco_firewall]
    event_types: [network_connection]
  paloaltonetworks:
    platforms: [network_appliance]
    data_sources: [palo_alto_firewall]
    event_types: [network_connection]
  fortinet:
    platforms: [network_appliance]
    data_sources: [fortinet_firewall]
    event_types: [network_connection]
  ciscoasa:
    platforms: [network_appliance]
    data_sources: [cisco_firewall]
    event_types: [network_connection]
  # ── GCP / Workspace ──────────────────────────────────────────────────
  gcpauditlogsdefinition:
    platforms: [gcp]
    data_sources: [gcp_audit]
    event_types: [api_call]
  googleworkspacereports:
    platforms: [google_workspace]
    data_sources: [google_workspace_audit]
    event_types: [audit_event]
  # ── Azure Monitor Agent (AMA) ingest connectors ──────────────────────
  # SyslogAMA is Linux syslog pulled in via Azure Monitor Agent — volume
  # is high (83 rules) because many third-party appliances fall back to
  # syslog when a native CEF/OCSF path doesn't exist.
  syslogama:
    platforms: [linux]
    data_sources: [linux_syslog]
    event_types: [audit_event]
  # CEF via AMA goes to the `CommonSecurityLog` table — dataType mapping
  # below carries the real telemetry signal.
  cefama:
    platforms: [network_appliance]
    data_sources: [palo_alto_firewall, fortinet_firewall, cisco_firewall]
    event_types: [network_connection]
  # Custom Logs via AMA is the catch-all for any custom-schema ingestion.
  # We don't know the shape, so cross_platform / application_logs.
  customlogsama:
    platforms: [cross_platform]
    data_sources: [application_logs]
    event_types: [audit_event]
  # ── Azure-native extras ──────────────────────────────────────────────
  # Web Application Firewall — usually via the AzureDiagnostics table.
  waf:
    platforms: [azure, network_appliance]
    data_sources: [azure_activity]
    event_types: [network_connection]
  # Microsoft Dataverse (Power Platform audit).
  dataverse:
    platforms: [microsoft_365]
    data_sources: [m365_audit]
    event_types: [audit_event]
  # Microsoft Defender for IoT.
  iot:
    platforms: [cross_platform]
    data_sources: [defender_cloud]
    event_types: [audit_event]
  # ── Third-party attack-path / AD security tools ──────────────────────
  bloodhoundenterprise:
    platforms: [windows]
    data_sources: [siem_alert]
    event_types: [alert_correlation]
  alsidforad:
    platforms: [windows]
    data_sources: [siem_alert]
    event_types: [audit_event]
  tenable.ad:
    platforms: [windows]
    data_sources: [siem_alert]
    event_types: [audit_event]
  tenableie:
    platforms: [windows]
    data_sources: [siem_alert]
    event_types: [audit_event]
  # ── Third-party SIEM alert feeds (long tail — `*_cl` dataType fallback
  # in the resolver catches anything not explicitly listed here) ───────
  cloudflaredataconnector:
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [network_connection]
  cyblevisionalerts:
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]
  cyfirmacyberintelligencedc:
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]
  boschaishield:
    platforms: [llm]
    data_sources: [siem_alert]
    event_types: [audit_event]
  authomize:
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]
  theom:
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]
  cognnisentineldataconnector:
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]
  senservapro:
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]
  sapbtpauditevents:
    platforms: [cross_platform]
    data_sources: [application_logs]
    event_types: [audit_event]
  42crunchapiprotection:
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [api_call]
  veeamcustomtablesdataconnector:
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]

# Per-dataType overrides. SecurityAlert is a generic table that any
# connector can produce, so detecting it at the data_type level lets us
# add the `siem_alert` data_source signal even when the connectorId is
# something we don't have a specific mapping for yet.
data_types:
  securityalert:
    data_sources: [siem_alert]
    event_types: [audit_event]
  awssecurityhubfindings:
    data_sources: [aws_security_hub]
    event_types: [audit_event]
  azureactivity:
    data_sources: [azure_activity]
    event_types: [api_call]
  signinlogs:
    data_sources: [entra_id_signin]
    event_types: [authentication]
  auditlogs:
    data_sources: [entra_id_audit]
    event_types: [audit_event]
  officeactivity:
    data_sources: [m365_audit]
    event_types: [audit_event]
  commonsecuritylog:  # CEF data pipe — usually firewall/proxy/NDR
    platforms: [network_appliance]
    event_types: [network_connection]
  azurediagnostics:  # Azure resource diagnostic logs
    platforms: [azure]
    data_sources: [azure_activity]
    event_types: [api_call]
  dataverseactivity:
    platforms: [microsoft_365]
    data_sources: [m365_audit]
    event_types: [audit_event]

# ─────────────────────────────────────────────────────────────────────────
# TIER 4 — SOLUTIONS FOLDER NAMES (vendor fallback)
# When `requiredDataConnectors` is missing and the KQL table didn't
# resolve, the `Solutions/<vendor>/` folder is an authoritative vendor
# signal. Kept lowercase for case-insensitive lookup. Only the top
# unmapped folders are listed here; long-tail vendors (single-rule
# folders) fall through to the `*_cl` table bucket.
# ─────────────────────────────────────────────────────────────────────────
solution_folders:
  # ── Drift 2026-08-30 (#64 + prod unknown-data-source audit).
  # Folders whose rules query ASIM functions or vendor _CL tables the
  # table tier cannot see. ──────────────────────────────────────────────
  "web session essentials":
    platforms: []
    data_sources: [proxy_logs]
    event_types: [http_request]
  "dns essentials":
    platforms: []
    data_sources: [dns_query_logs]
    event_types: [dns_query]
  "network session essentials":
    platforms: []
    data_sources: [network_traffic_logs]
    event_types: [network_connection]
  "corelight":
    platforms: [network_appliance]
    data_sources: [zeek]
    event_types: [network_connection]
  "iboss":
    platforms: [network_appliance]
    data_sources: [proxy_logs]
    event_types: [http_request]
  "microsoft copilot":
    platforms: [microsoft_365]
    data_sources: [m365_audit]
    event_types: [audit_event]
  "azure firewall":
    platforms: [azure]
    data_sources: [azure_firewall]
    event_types: [network_connection]
  "googlecloudplatformdns":
    platforms: [gcp]
    data_sources: [gcp_dns]
    event_types: [dns_query]
  "googlecloudplatformiam":
    platforms: [gcp]
    data_sources: [gcp_audit]
    event_types: [audit_event]
  "googleworkspacereports":
    platforms: [google_workspace]
    data_sources: [google_workspace_audit]
    event_types: [audit_event]
  "paloaltoprismacloud":
    platforms: []
    data_sources: [prisma_cloud_audit]
    event_types: [platform_alert]
  "cyberarkepm":
    platforms: [windows]
    data_sources: [cyberark_epm]
    event_types: [audit_event]
  "atlassianjiraaudit":
    platforms: [atlassian]
    data_sources: [atlassian_audit]
    event_types: [audit_event]
  "trend micro cloud app security":
    platforms: [email]
    data_sources: [trend_micro_cas]
    event_types: [platform_alert]
  "bitglass":
    platforms: []
    data_sources: [proxy_logs]
    event_types: [http_request]
  "oracle cloud infrastructure":
    platforms: [oci]
    data_sources: [oci_audit]
    event_types: [audit_event]
  "snowflake":
    platforms: [snowflake]
    data_sources: [snowflake_query_history, snowflake_login_history]
    event_types: [audit_event]
  "impervacloudwaf":
    platforms: [network_appliance]
    data_sources: [imperva_waf]
    event_types: [http_request]
  "radiflow":
    platforms: [network_appliance]
    data_sources: [third_party_security_alerts]
    event_types: [platform_alert]
  "sailpointidentitynow":
    platforms: []
    data_sources: [sailpoint_idn_audit]
    event_types: [audit_event]
  "microsoft purview":
    platforms: [microsoft_365]
    data_sources: [m365_purview_audit]
    event_types: [audit_event]
  "salesforce service cloud":
    platforms: [salesforce]
    data_sources: [salesforce_realtime]
    event_types: [audit_event]
  "lookout":
    platforms: []
    data_sources: [third_party_security_alerts]
    event_types: [platform_alert]
  "knowbe4 defend":
    platforms: [email]
    data_sources: [third_party_security_alerts]
    event_types: [platform_alert]
  "gravityzone":
    platforms: []
    data_sources: [third_party_security_alerts]
    event_types: [platform_alert]
  "extrahop":
    platforms: [network_appliance]
    data_sources: [third_party_security_alerts]
    event_types: [platform_alert]
  "dataminr pulse":
    platforms: []
    data_sources: [third_party_security_alerts]
    event_types: [platform_alert]
  "threatconnect":
    platforms: []
    data_sources: [third_party_security_alerts]
    event_types: [platform_alert]
  "honeylabs":
    platforms: []
    data_sources: [third_party_security_alerts]
    event_types: [platform_alert]
  "datazag":
    platforms: []
    data_sources: [dns_query_logs]
    event_types: [dns_query]
  "synqlyintegrationconnector":
    platforms: []
    data_sources: [siem_alert]
    event_types: [platform_alert]
  "windows forwarded events":
    platforms: [windows]
    data_sources: [windows_event_logs]
    event_types: [audit_event]
  "microsoft exchange security - exchange on-premises":
    platforms: [windows]
    data_sources: [windows_event_logs]
    event_types: [audit_event]
  "fortinet fortiweb cloud waf-as-a-service connector for microsoft sentinel":
    platforms: [network_appliance]
    data_sources: [fortinet_firewall]
    event_types: [http_request]
  # Compliance packs: rules re-query other rules' output / built-in
  # SIEM tables; the honest source is the SIEM itself.
  "cybersecuritymaturitymodelcertification(cmmc)2.0":
    platforms: []
    data_sources: [siem_alert]
    event_types: [audit_event]
  "continuousdiagnostics&mitigation":
    platforms: []
    data_sources: [siem_alert]
    event_types: [audit_event]
  "zerotrust(tic3.0)":
    platforms: []
    data_sources: [siem_alert]
    event_types: [audit_event]
  "nistsp80053":
    platforms: []
    data_sources: [siem_alert]
    event_types: [audit_event]
  "business email compromise - financial fraud":
    platforms: []
    data_sources: [siem_alert]
    event_types: [audit_event]
  "ciscoduosecurity":
    platforms: [duo]
    data_sources: [duo_activity]
    event_types: [authentication]
  "mimecast":
    platforms: [email]
    data_sources: [email_message_metadata]
    event_types: [platform_alert]
  "dynatrace":
    platforms: []
    data_sources: [third_party_security_alerts]
    event_types: [platform_alert]
  "commvault security iq":
    platforms: []
    data_sources: [third_party_security_alerts]
    event_types: [platform_alert]
  "microsoft defender xdr":
    platforms: [microsoft_365]
    data_sources: [microsoft_defender_xdr]
    event_types: [platform_alert]
  # ── SOCRadar (alarms / incidents feed; #56) ──────────────────────────
  "socradar":
    platforms: [socradar]
    data_sources: [socradar_incidents]
    event_types: [platform_alert]
  # ── Maturity Model for Event Log Management (M-21-31) ──────────────
  # Rules carry no connector at all; they audit the SIEM's own coverage.
  "maturitymodelforeventlogmanagementm2131":
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]
  # ── Acronis ─────────────────────────────────────────────────────────
  "acronis cyber protect cloud":
    platforms: [cross_platform]
    data_sources: [antivirus_logs]
    event_types: [audit_event]
  # ── Azure DevOps ────────────────────────────────────────────────────
  "azuredevopsauditing":
    platforms: [cross_platform]
    data_sources: [application_logs]
    event_types: [audit_event]
  # ── CTERA (edge filers) ─────────────────────────────────────────────
  "ctera":
    platforms: [cross_platform]
    data_sources: [antivirus_logs]
    event_types: [audit_event]
  # ── Veritas NetBackup ───────────────────────────────────────────────
  "veritas netbackup":
    platforms: [cross_platform]
    data_sources: [application_logs]
    event_types: [audit_event]
  # ── VMware appliances ───────────────────────────────────────────────
  "vmware esxi":
    platforms: [cross_platform]
    data_sources: [linux_syslog]
    event_types: [audit_event]
  "vmware sd-wan and sase":
    platforms: [network_appliance]
    data_sources: [application_logs]
    event_types: [network_connection]
  # ── Entra ID (falls through kql_tables already, but in case) ────────
  "microsoft entra id":
    platforms: [azure]
    data_sources: [entra_id_signin, entra_id_audit]
    event_types: [authentication, audit_event]
  # ── Microsoft Business Applications (Dataverse / PowerPlatform) ─────
  "microsoft business applications":
    platforms: [microsoft_365]
    data_sources: [m365_audit]
    event_types: [audit_event]
  # ── Microsoft 365 misc ──────────────────────────────────────────────
  "microsoft 365":
    platforms: [microsoft_365]
    data_sources: [m365_audit]
    event_types: [audit_event]
  # ── Threat intelligence groups ──────────────────────────────────────
  "threat intelligence":
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]
  "threat intelligence (new)":
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]
  # ── AWS ─────────────────────────────────────────────────────────────
  "amazon web services":
    platforms: [aws]
    data_sources: [aws_cloudtrail]
    event_types: [api_call]
  # ── GitHub ──────────────────────────────────────────────────────────
  "github":
    platforms: [github]
    data_sources: [github_audit]
    event_types: [audit_event]
  # ── Endpoint detection vendors (fold to siem_alert by convention) ───
  "bloodhound enterprise":
    platforms: [windows]
    data_sources: [siem_alert]
    event_types: [alert_correlation]
  "cyble vision":
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]
  "cyfirma cyber intelligence":
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]
  "cyfirma digital risk":
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]
  "cyfirma attack surface":
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]
  "aishield ai security monitoring":
    platforms: [llm]
    data_sources: [siem_alert]
    event_types: [audit_event]
  "authomize":
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]
  "theom":
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]
  "senservapro":
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]
  "cognni":
    platforms: [cross_platform]
    data_sources: [siem_alert]
    event_types: [audit_event]
  "alsid for ad":
    platforms: [windows]
    data_sources: [siem_alert]
    event_types: [audit_event]
  # ── Veeam (backup software — Windows server running the backup,
  # forwarding events over syslog) ───────────────────────────────────
  "veeam":
    platforms: [windows, cross_platform]
    data_sources: [application_logs]
    event_types: [audit_event]
  # ── Windows Security Events (Microsoft native) ──────────────────────
  "windows security events":
    platforms: [windows]
    data_sources: [windows_security_event_log]
    event_types: [audit_event]
  # ── FalconFriday (CrowdStrike) ──────────────────────────────────────
  "falconfriday":
    platforms: [windows, linux, macos]
    data_sources: [crowdstrike_fdr]
  # ── Azure Activity ──────────────────────────────────────────────────
  "azure activity":
    platforms: [azure]
    data_sources: [azure_activity]
    event_types: [api_call]
  # ── SAP BTP ─────────────────────────────────────────────────────────
  "sap btp":
    platforms: [cross_platform]
    data_sources: [application_logs]
    event_types: [audit_event]
  # ── IoT / OT ────────────────────────────────────────────────────────
  "iotottreatmonitoringwithdefenderforiot":
    platforms: [cross_platform]
    data_sources: [defender_cloud]
    event_types: [audit_event]

# ─────────────────────────────────────────────────────────────────────────
# TIER 5 — ENTITY TYPES (last-resort event_type hint)
# Only consulted when no higher tier set any event_type. Entity types
# describe what the rule RETURNS, not where data comes from — so we
# never let them set platforms or data_sources.
# ─────────────────────────────────────────────────────────────────────────
entity_types:
  mailmessage:
    event_types: [email_message]
  mailbox:
    event_types: [email_message]
  process:
    event_types: [process_creation]
  file:
    event_types: [file_event]
  filehash:
    event_types: [file_event]
  account:
    event_types: [authentication]
  registrykey:
    event_types: [registry_event]
  registryvalue:
    event_types: [registry_event]
  ip:
    event_types: [network_connection]
  dnsresolution:
    event_types: [dns_query]
  url:
    event_types: [http_request]

# Sentinel rules are SIEM correlation queries by definition — every rule
# is examining audit/api activity at minimum.
always_includes:
  event_types: []
