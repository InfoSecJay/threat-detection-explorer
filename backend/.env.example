# Threat Detection Explorer - Backend Configuration
# Copy this file to .env and customize for your environment

# ===================
# Environment
# ===================
# Options: development, staging, production
ENVIRONMENT=development

# ===================
# Database
# ===================
# Local development (SQLite):
# DATABASE_URL=sqlite+aiosqlite:///./data/threat_detection.db

# Production (PostgreSQL on Railway):
# DATABASE_URL=postgresql+asyncpg://username:password@host:port/database

# Local Postgres holding a production snapshot (#38; docker compose up,
# then python scripts/dev_db.py refresh). Run the dev server against it:
# DATABASE_URL=postgresql+asyncpg://detection_explorer:detection_explorer@localhost:5433/detection_explorer

# ===================
# CORS (Frontend URL)
# ===================
# Your deployed frontend URL (Vercel)
# FRONTEND_URL=https://your-app.vercel.app

# Additional CORS origins (comma-separated)
# CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# ===================
# Scheduler
# ===================
# Enable automatic daily sync
ENABLE_SCHEDULER=true

# Time to run daily sync (UTC)
SYNC_SCHEDULE_HOUR=2
SYNC_SCHEDULE_MINUTE=0

# ===================
# Debug
# ===================
# Enable debug logging (auto-disabled in production)
DEBUG=true
